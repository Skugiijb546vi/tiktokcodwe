name: Process Movie and Send to Telegram

on:
  workflow_dispatch:
    inputs:
      video_url:
        description: 'URL of the video to process'
        required: true

jobs:
  process:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3
    - uses: actions/setup-python@v4
      with:
        python-version: '3.10'
    - run: pip install -r requirements.txt
    - run: sudo apt-get update && sudo apt-get install -y ffmpeg
    - run: playwright install
    - env:
        API_ID: ${{ secrets.API_ID }}
        API_HASH: ${{ secrets.API_HASH }}
        BOT_TOKEN: ${{ secrets.BOT_TOKEN }}
        CHANNEL_ID: ${{ secrets.CHANNEL_ID }}
        TIKTOK_COOKIES: ${{ secrets.TIKTOK_COOKIES }}
      run: |
        echo "$TIKTOK_COOKIES" > cookies.txt
        python scripts/process_and_send.py "${{ github.event.inputs.video_url }}"
        python scripts/tiktok_cron_upload.py
